# @package _global_

defaults:
  - override /trainer: trainer
  - override /logger: mlflow
  - override /optimizer: adam
  - override /transforms: geometric_higher_prob

trainer:
  max_epochs: 300

optimizer:
  lr: 0.005

logger:
  run_name: "layers:6_skipcon:8_lr:0.005_epochs:300_transf:higher-prob_scheduler"

